<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.polettiapps.lucaspoletti.math">

    <application
        android:allowBackup="true"
        android:icon="@drawable/icone_app"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".Activity.MainActivity"
            android:icon="@drawable/icone_app"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".Activity.CalculadoraRegraDeTresSimplesActivity"
            android:label="Regra de Três Simples" />
        <activity
            android:name=".Activity.CalculadoraPotenciaActivity"
            android:label="Potências" />
        <activity
            android:name=".Activity.CalculadoraRaizesActivity"
            android:label="Raíz Quadrada" />
    </application>

</manifest>